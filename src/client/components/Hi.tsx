import React from 'react';

const Hi = () => {
    return (
        <div>
            Hi, I am a React component! This is a simple example of a React component that renders a greeting message. You can customize this component by adding props, state, or other features as needed.
        </div>
    );
};

export default Hi;